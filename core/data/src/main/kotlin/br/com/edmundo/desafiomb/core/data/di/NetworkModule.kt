package br.com.edmundo.desafiomb.core.data.di

import org.koin.dsl.module

/** Json, interceptors, OkHttpClient, Retrofit e CmcExchangeApi (spec 3.1). Preenchido na E2. */
val networkModule = module {
    // E2
}
